# 因子台帳 — 設計メモ (SPEC)

ウマ娘の因子（青/赤/白）を画像から読み取って登録・検索できる個人用ツール。
現状はClaudeアーティファクト環境で動作するプロトタイプ。ここではGitHub版に移行する際の設計方針をまとめる。

## 現状のプロトタイプ

- 単一HTMLファイル（HTML / CSS / JS 一枚構成）
- 画像貼り付け → Anthropic APIに画像を送信 → JSONで因子情報を抽出 → フォームに自動入力
- 保存/検索/削除の基本機能あり
- データ保存はClaude環境の `window.storage`（Claudeアカウント紐づき、環境外では動作しない）

## データ構造

1エントリ（1頭）のJSON形式:

```json
{
  "id": "factor_xxxxx",
  "character": "スペシャルウィーク",
  "blue": { "name": "スタミナ", "level": 3 },
  "red": [{ "name": "シナリオ因子名", "level": 2 }],
  "white": [{ "name": "白因子名", "level": 1 }],
  "notes": "用途メモ",
  "savedAt": "ISO8601形式の日時"
}
```

一覧は上記エントリの配列を、リポジトリ内の1つのJSONファイル（例: `data/factors.json`）にまとめて保存する方針。

## GitHubをDB代わりにする方針

### 読み込みタイミング（ポーリングしない）
- アプリを開いた時に1回、GitHub Contents APIから `factors.json` を取得
- 取得したデータをメモリ上に保持し、**検索・フィルタはこのローカルデータに対してのみ**行う（キー入力のたびにAPIは叩かない）
- 他端末の更新を取り込みたい時のために「更新」ボタンを用意し、手動で再取得できるようにする

### 書き込みタイミング
- 保存 / 削除の操作をした時のみGitHubへ書き込む
- 書き込み前に必ず最新の `sha` を取得し直してから更新する（Contents APIの仕様）
- 他端末が先に更新していた場合は409エラーになるので、その場合は「最新を取得し直してもう一度保存してください」という案内を出す

### 想定される速度・制約
- 1回の読み書き自体は数百ms程度で、単発の体感ラグは小さい
- リアルタイム同期の仕組みはないため、他端末の変更には気づけない（手動更新前提でOKという結論済み）
- レート制限: 認証済みトークンで1時間5,000リクエストまで。個人利用・上記の読み込みタイミングなら十分余裕あり
- 保存のたびにコミットが積まれる点は想定内として許容する

## 画像解析（Anthropic API）について

- Claudeアーティファクト内ではAPIキー不要で呼び出せていたが、GitHub Pages等の公開環境ではAPIキーが必要
- キーをクライアント側JSに直接埋め込むと誰でも見える状態になるため、**簡易バックエンド（Vercelのサーバーレス関数など）を挟んでキーを隠す**必要がある
- バックエンドは「画像を受け取ってAnthropic APIに投げてJSONを返すだけ」のシンプルな中継で良い

## 移行時のファイル構成案

```
/
├── index.html
├── style.css
├── script.js
├── data/
│   └── factors.json        # 因子データ本体
├── api/
│   └── analyze.js（等）      # 画像解析の中継バックエンド（APIキーはここで管理）
└── README.md
```

## 未決事項

- リポジトリを公開にするか非公開にするか（非公開ならGitHub Pagesでの公開方法や認証トークンの扱いも検討要）
- GitHubへの書き込み用トークンをどこに保存するか（ブラウザのローカルストレージに個人用PATを保存する想定でひとまず問題ない想定だが、要確認）
- ホスティング先（GitHub Pages想定、他候補があれば要相談）

## 実装時の決定事項（このメモからの変更点）

- **リポジトリは公開(Public)にした**。因子データのみで個人情報は載らないため。GitHubへの書き込み用PATはブラウザの`localStorage`に保存する方針のまま採用。
- **画像解析（Anthropic API連携）は採用しなかった**。個人の趣味ツールにAPI課金までは不要と判断。代わりに、Claudeアプリ（chat）にスクリーンショットと抽出用プロンプトを渡し、返ってきたJSONをアプリ画面に手動で貼り付けてフォームへ反映する方式にした。そのため `api/` ディレクトリ（Vercelサーバーレス関数）は不要になり、リポジトリから削除した。
- 上記の結果、バックエンドが一切不要な完全静的サイトになったため、**ホスティングはGitHub Pages**を採用した（Vercelは不使用）。
